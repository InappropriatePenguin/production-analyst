require("scripts/constants")

data:extend{
    {
        name = NAME.setting.overhead_button,
        setting_type = "runtime-per-user",
        order = "a",
        type = "bool-setting",
        default_value = true
    }
}